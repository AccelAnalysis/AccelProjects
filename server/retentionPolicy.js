export const retentionPolicies = Object.freeze({
  operational_temporary: { restoreDays: 30, purgeAfterDays: 30, purgeMode: "schedulable", immutable: false }, ordinary_project: { restoreDays: 30, purgeAfterDays: 90, purgeMode: "manual", immutable: false }, project_trash: { restoreDays: 30, purgeAfterDays: 30, purgeMode: "manual", immutable: false }, relationship_30d: { restoreDays: 30, purgeAfterDays: 30, purgeMode: "schedulable", immutable: false }, communication_history: { restoreDays: null, purgeAfterDays: null, purgeMode: "never", immutable: true }, calendar_history: { restoreDays: null, purgeAfterDays: null, purgeMode: "never", immutable: true }, approved_report: { restoreDays: null, purgeAfterDays: null, purgeMode: "never", immutable: true }, report_artifact: { restoreDays: null, purgeAfterDays: null, purgeMode: "never", immutable: true }, contract_billing: { restoreDays: null, purgeAfterDays: null, purgeMode: "manual", immutable: false }, audit_permanent: { restoreDays: null, purgeAfterDays: null, purgeMode: "never", immutable: true }, legal_hold: { restoreDays: null, purgeAfterDays: null, purgeMode: "never", immutable: true }, operational_30d: { restoreDays: 30, purgeAfterDays: 30, purgeMode: "schedulable", immutable: false }, business_7y: { restoreDays: 30, purgeAfterDays: null, purgeMode: "manual", immutable: false }
});

export function retentionDates(retentionClass, from = new Date()) { const policy = retentionPolicies[retentionClass] || retentionPolicies.ordinary_project; const add = (days) => days == null ? null : new Date(from.getTime() + days * 86400000).toISOString(); return { restoreDeadline: add(policy.restoreDays), purgeEligibleAt: add(policy.purgeAfterDays) }; }
